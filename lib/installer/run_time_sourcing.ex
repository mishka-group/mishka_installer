defmodule MishkaInstaller.Installer.RunTimeSourcing do
  def get_installed_libs(_app) do

  end

  def compiled_libs(_mode \\ Mix.env() || :dev) do

  end

  def compare_dependencies(_installed_libs, _new_compiled) do

  end
end
